#include <cstdio>
#include <vector>
#include <cstring>
#include <iostream>
#include <algorithm>

using namespace std;


class Solution {
public:
    struct node
    {
        int idx;
    };
    
    long long maximumTotalDamage(vector<int>& power) {
        sort(power.begin(), power.end());
        
    }
};

int main() {
    return 0;
}